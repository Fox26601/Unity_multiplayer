namespace FusionMultiplayer.Chat
{
    /// <summary>
    /// Chat tuning constants (local only).
    /// </summary>
    public static class ChatMessage
    {
        /// <summary>Matches <see cref="ChatManager"/> RPC NetworkString capacity.</summary>
        public const int MaxLength = 64;
    }
}
